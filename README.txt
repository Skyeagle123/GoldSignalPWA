GoldSignalsPack v2 - Improved
What's changed:
- SSE reconnection backoff + polling fallback
- Sanity spike filtering (ignore >2% sudden jumps)
- CSV age check and UI badge
- Browser Notifications support (permission prompt happens in browser)
- Added manifest and service worker (sw.js) for basic PWA behavior
- Worker improved to combine multiple live sources and longer stream window
